---
inclusion: fileMatch
fileMatchPattern: "**/*.{js,jsx,ts,tsx}"
---

# Lint

Após gerar ou modificar arquivos `.js`, `.jsx`, `.ts` ou `.tsx`, verificar se o código está alinhado com o `tsconfig.json` do projeto correspondente (frontend ou backend).

No frontend, rodar `npm run lint` para validar.
